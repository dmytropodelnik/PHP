‘ункции в PHP

‘ункции объ€вл€ютс€ командой function, синтаксис традиционный,
имена функций - без $, параметры - с $.
ѕерегрузка запрещена, нельз€ создать несколько функций 
с одинаковым именем. ѕараметрический полиморфизм реализуетс€ 
параметрами по умолчанию.

function print_hello( $name = null ) {...}
...
print_hello( ) ;
print_hello( "User" ) ;
√лобальные переменные в функци€х напр€мую не видны.
ƒл€ доступа к ним, в теле функции добавл€етс€ инструкци€
 global $x, $y;

Ѕиблиотеки / модули / файлы
ƒл€ распределени€ кода по част€м, его разбивают на файлы,
обычно, оформл€€ в виде функций.
ѕодключаютс€ файлы одной из команд:
include "file.name" ;  // "вставка" содержимого файла в код
// переменные из файла видны в подключающем файле, 
// относительные пути сохран€ютс€ от стартового файла (выполн€ющего).
include_once "file.name" ;  // однократное подключение
// если файл ранее подключалс€, инструкци€ игнорируетс€
// иначе - как include 
// используетс€, если в файле определ€ютс€ функции/классы (т.к.
// их переопределение вызывает ошибку)
require "file.name" ;  // аналог include, но, в случае ошибки 
// подключени€, приводит к фатальной ошибке
// include - к предупреждению
require_once "file.name" ;  // однократное подключение
